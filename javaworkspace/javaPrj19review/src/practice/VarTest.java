package practice;

//변수복습
public class VarTest {
	
	/*
	 * 변수, 선언, 할당, 초기화
	 * 변수 명명규칙  -> camelCase ☆, $, _ + 특수문자x, 공백x, 명사형태(ex : 나이, 이름 등), 간략히 
	 * 상수
	 * 주의사항  -> 이름 중복 불가능 (ex : int int = ~~)
	 * 변수의 스코프, 지역변수  -> 변수의 스코프 : 변수가 살아있는 위치, 지역변수 : 
	 * 상수, final - 재할당불가능  -> 상수 : 변하지 않는 값 + final이 붙으면 = 재할당불가능한 값이 됨
	 * 기본값  -> 참조자료형 의 경우 : 전부 기본값이 null 이다. null = 0번지 라는 뜻
	 */
	
	public void test() {
		
		
		
		int x;
		x = 10;
		
		System.out.println(x);
		
	}
	

}
